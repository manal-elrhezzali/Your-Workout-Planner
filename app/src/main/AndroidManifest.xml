<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.elrhezzalimanal.yourworkoutplanner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourWorkoutPlanner"
        android:usesCleartextTraffic="true"> <!-- added this so that the WebView will be able to load sites that are HTTP, not only load an HTTPS sites -->
        <activity android:name=".WebsiteActivity"></activity>
        <activity android:name=".EditActivity" />
        <activity android:name=".PlanActivity" />
        <activity android:name=".TrainingActivity" />
        <activity android:name=".AllTrainingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>